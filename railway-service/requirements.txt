fastapi==0.115.0
uvicorn[standard]==0.30.6
opencv-python-headless==4.10.0.84
numpy==1.26.4
httpx==0.27.2
yt-dlp==2024.9.27
supabase==2.8.1
python-multipart==0.0.9
python-dotenv==1.0.1
scenedetect[opencv]>=0.6.4
# Pinned: ultralytics==8.3.40 bundles a specific yolo11n.pt checkpoint, so
# pinning the package effectively pins the weights we ship to Railway.
ultralytics==8.3.40
onnxruntime>=1.18.0
# rtmlib is the dependency-light wrapper around RTMPose ONNX models.
# Pinned so the model URL it downloads (RTMPose-m body7) doesn't move
# under us between deploys.
rtmlib==0.0.15
